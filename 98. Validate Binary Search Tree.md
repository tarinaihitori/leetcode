1st
最近、木の問題を多く解いていたので、再帰が一番初めに思いついた。

```python
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        def _is_valid_bst(node, low =-(math.inf), high = math.inf):
            if node is None:
                return True
            if not (low < node.val < high):
                return False
            return _is_valid_bst(node.left, low, node.val) and _is_valid_bst(node.right, node.val, high)
        return _is_valid_bst(root)
```

2nd
dequeを用いた DFS（stackを使ってもいい）
今回の制約上、root に None が来ることはないが、もし None が引数として渡された場合、True か False どちらを返すべきなのだろうか。
論理的に考えれば、True を返すべきだと思うが、気になる。
調べた結果、True を返すのが正しそう。
<https://en.wikipedia.org/wiki/Vacuous_truth>
<https://zenn.dev/wsuzume/articles/c4f85df441def6>

```python
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        if root is None:
            return True
        
        nodes_with_bounds = deque([(root, -(math.inf), math.inf)])
        
        while nodes_with_bounds:
            current_node, lower_bound, upper_bound = nodes_with_bounds.popleft()
            
            if not (lower_bound < current_node.val < upper_bound):
                return False
            if current_node.left is not None:
                nodes_with_bounds.append((current_node.left, lower_bound, current_node.val))
            if current_node.right is not None:
                nodes_with_bounds.append((current_node.right, current_node.val, upper_bound))
        
        return True
```

ほかの人のコードを読んだ。
math.infはfloat型なのでnode.valがint型と型の不一致が気になるという意見があった。
sys.maxsizeを使えば解決できるらしい。好みと意見もあったが、せっかくなら型を合わせたいからsys.maxsizeがよさそう。
<https://docs.python.org/ja/3/library/sys.html#sys.maxsize>
あとinorder traversalというものがあったが単語自体知らなかったので、調べる。
ノードを「左->ルート->右」の順序で訪問する方法だそう。
<https://note.com/ym202110/n/n7daa8a8cc3d6>

<https://github.com/Fuminiton/LeetCode/pull/28/files>

in-order traversalを用いて書いてみるが、書けなかった。
コードを読んでなぜそれで動くのかはわかるが、

```python
cclass Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        if root is None:
            return True
        
        node_stack = []
        node = root
        prev_val = -(sys.maxsize)

        while node_stack or node is not None:
            while node:
                node_stack.append(node)
                node = node.left
            node = node_stack.pop()
            if node.val <= prev_val:
                return False
            prev_val = node.val
            node = node.right
        return True       
```

BFS

```python
from collections import deque

class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        if root is None:
            return True
        queue = deque([(root, float('-inf'), float('inf'))])
        while queue:
            node, low, high = queue.popleft()
            if not node:
                continue
            val = node.val
            if not (low < val < high):
                return False
            queue.append((node.left, low, val))
            queue.append((node.right, val, high))
        return True

```

3rd

```python
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        def is_valid_bst_helper(node, low=-(sys.maxsize), high=sys.maxsize):
            if node is None:
                return True
            if not (low < node.val < high):
                return False
            return is_valid_bst_helper(node.left, low, node.val) \
            and is_valid_bst_helper(node.right, node.val, high)
        return is_valid_bst_helper(root)
```
