1st
考えたが思いつかなかったので、答えを見た。
ちなみにみたのはこれ。<https://leetcode.com/problems/longest-increasing-subsequence/solutions/4509129/99-54-easy-solution-with-explanation>
「位置 i で終わる増加部分列の最長長さ」を状態として定義すると、
末尾を i に固定したとき、その長さは “i より前にあって、かつ nums[j] < nums[i] を満たす j の中で最も長い増加部分列長 + 1”
という形で再帰的に書けるとわかった。

subsequence（部分列） とは、ある列（シーケンス）から 一部の要素を取り除いたり、取り除かずに 順番を変えずに残したときに得られる列のこと。
元の列に含まれる要素の順番を変えてはいけない。
ただし、「要素を飛ばす（削除する）」ことはできる。
すべての要素を削除しなかったとしても削除してもよく、極端な例として、何も削除しないでそのままの列も部分列の一つ。

```python
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        if not nums:
            return 0
        n = len(nums)
        dp = [1] * n

        for i in range(1, n):
            for j in range(i):
                if nums[i] > nums[j]:
                    dp[i] = max(dp[i], dp[j] + 1)
        return max(dp)
```

2nd
dpやdfsという変数名をみたら、「もうちょっと意味のある名前をつけませんか？」という小田さんのコメントが頭の中で再生されるので、
変数名も含めて読みやすいように変えてみる。
最初の`if not nums`はこのままだと、numsにNoneが渡されても0を返してしまう。型ヒントからnumsにNoneは来ないことがわかっているが、
もし来た場合、例外を出して早めにバグを顕在化してほしいなと思ったので、TypeErrorを出すようにした。
あと最近Design by Contractとか防御的プログラミングの概念を本(Good Code, Bad Code)で学習したので、このあたりはチームの方針にもよるのかなと思った。
`if nums...`の箇所は<（小なり）のほうが読みやすいので、<で書くようにした。

```python
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        if nums is None:
            raise TypeError("nums must be a List, not None")
        if len(nums) == 0:
            return 0
        
        nums_length = len(nums)
        lis_length = [1] * nums_length

        for i in range(1, nums_length):
            for j in range(i):
                if nums[j] < nums[i]:
                    lis_length[i] = max(lis_length[i], lis_length[j] + 1)
        return max(lis_length)
    
```

ほかの人のコードを読むタイム
<https://github.com/Fuminiton/LeetCode/pull/31/files>
<https://github.com/fuga-98/arai60/pull/31/files>
<https://github.com/Mike0121/LeetCode/pull/50/files>

二分探索でもいけるらしい。Patience Sortingともいうのかな。
<https://en.wikipedia.org/wiki/Patience_sorting>
<https://qiita.com/kgoto/items/d1e9d39b8b391fc19970>
トランプの山を複数作るイメージ
リスト（配列）の各要素を順に見ていき、下記のルールで「山」にカードを置いていく。
「山の先頭要素」より小さければ差し替え、そうでなければ新たな山を作る
ある要素 x を配置するとき、今までできている山のうち「先頭要素が x 以上となる最初の山」に x を置き換える（その山の先頭要素を x に置き換えるイメージ）。
もしそのような山が存在しなければ、新しい山を作ってそこに x を置く。
山の数が LIS の長さになる
結果的にできあがった山の「数」が、元の配列における最長増加部分列の長さ（LIS）になる。
これだと、`nums`のループとループごとの二分探索で済むので、O(n log n) に時間計算量を短縮できる。

```python
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        if not nums:
            return 0
        tails = []
        for num in nums:
            index = bisect_left(tails, num)
            if index == len(tails):
                tails.append(num)
            else:
                tails[index] = num
        return len(tails)
```

二分探索ぐらい、ソラで書けた方がいいよねと思うので、
自分で二分探索を書く。
pythonのintはオーバーフローしないが、念の為midを求めるやり方はオーバーフローを考慮したものにする。
二分探索の実装も見てみる
<https://github.com/python/cpython/blob/3.13/Lib/bisect.py#L74>

```python
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        tails = []
        for num in nums:
            left = 0
            right = len(tails)
            while left < right:
                mid = left + (right - left) // 2
                if tails[mid] < num:
                    left = mid + 1
                else:
                    right = mid
            if left == len(tails):
                tails.append(num)
            else:
                tails[left] = num
        return len(tails)
```

ほかにも Fenwick Tree(Binary Indexed Tree)やセグメントツリーを使っても解けるらしいが、
オーバーエンジニアリングっぽかったのとソフトウェアエンジニアリングの常識からは外れていそうだったので、それらのコードは書いていません。
<https://en.wikipedia.org/wiki/Fenwick_tree>

3rd
二分探索を自分で実装するとバグる可能性が高いので、ライブラリを使う方法で書いた。

```python
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        if nums is None:
            raise TypeError("nums must be a List, not None")
        if len(nums) == 0:
            return 0
        
        tails = []
        for num in nums:
            insert_position = bisect_left(tails, num)
            if insert_position == len(tails):
                tails.append(num)
            else:
                tails[insert_position] = num
        return len(tails)
```
